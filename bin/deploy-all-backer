#! /usr/bin/env bash
BIN_DIR=${BIN_DIR:-$(cd "${0%/*}"&&pwd)}

# Define currency address like DAI ERC20
export CURRENCY=0x00000000000000000000

# Define nft registry for deployment
export NFT_COLLATERAL=0x00000000000000000000

export CURRENCY_COLLATERAL_DEPLOYED=1
$BIN_DIR/deploy-basic-backer


